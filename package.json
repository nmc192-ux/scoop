{
  "name": "scoop-news",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:local": "./scripts/build-scoop.sh",
    "start:local": "./scripts/start-scoop.sh",
    "build": "npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
